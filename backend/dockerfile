FROM python:3.12.2
WORKDIR /backend

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

ENV POSTGRES_DB=PCT
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=Danny100
ENV POSTGRES_HOST=postgres
ENV POSTGRES_PORT=5432

COPY . .

RUN pip install -r requirements.txt

EXPOSE 8000
